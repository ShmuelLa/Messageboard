#include <string>
#include "Board.hpp"
#include "Direction.hpp"
using namespace std;

namespace ariel {
    void Board::post(unsigned int row, unsigned int column, Direction direction, string content) {
        
    }

    string Board::read(unsigned int row, unsigned int column, Direction direction, unsigned int length) {
        string result = "s";
        return result;
    }
    
    void Board::show() {

    }
}